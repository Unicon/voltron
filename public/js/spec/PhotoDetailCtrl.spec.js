define(
	[
		'app',
		'controller/PhotoDetailCtrl',
		'resource/Photo'
	],
	function (app, PhotoDetailCtrl, Photo) {

		describe('PhotoDetailCtrl.', function () {
			it('should do something.', function () {
				var scope, routeParams, controller;

				//scope = {},
				//routeParams = {photoId: 200};
				//controller = new PhotoDetailCtrl(scope, routeParams, Photo);
			});
		});

	}
);